import {FormControl} from '@angular/forms';

export type RegisterResponse = {
  password: string
  role: string
  name: string
  phoneNumber: string
  email: string
  address: string
  city: string
  uf: string
}
